# This application expose one "GET" rest endpoint to check path is exist between two cities

http://localhost:8080/connected?origin=Boston&destination=Newark

Check out project as Maven and build and run it
